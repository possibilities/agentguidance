interface:
  display_name: "Notify"
  short_description: "Reach the human with a macOS notification"
  default_prompt: "Use $notify to post a macOS notification when the human is away from the terminal."
