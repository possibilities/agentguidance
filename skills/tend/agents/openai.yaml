interface:
  display_name: "Tend"
  short_description: "Triage inactive Herdr worktrees without changing them"
  default_prompt: "Use $tend to watch inactive Herdr worktrees and propose safe cleanup or catch-up work without performing it."
